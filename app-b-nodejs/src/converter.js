/**
 * RED STAGE stub - both functions return 0 so every test fails initially.
 *
 * GREEN STAGE: replace with real logic, e.g.
 *   metersToFeet(meters) {
 *     if (meters === null || meters === undefined || typeof meters !== 'number' || isNaN(meters))
 *       throw new Error('Input must be a valid number');
 *     if (meters < 0)
 *       throw new Error('Input must be non-negative');
 *     return meters * 3.28084;
 *   }
 */

function metersToFeet(meters) {
  // RED: stub
  return 0;
}

function feetToMeters(feet) {
  // RED: stub
  return 0;
}

module.exports = { metersToFeet, feetToMeters };
